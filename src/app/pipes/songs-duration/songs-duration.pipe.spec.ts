import { SongsDurationPipe } from './songs-duration.pipe';

describe('SongsDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new SongsDurationPipe();
    expect(pipe).toBeTruthy();
  });
});
